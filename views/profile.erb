<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <link rel="shortcut icon" href="/img/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/img/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/img/favicon-16x16.png">
  </head>
  <body>
    <img src="/img/id-card.svg" height="64" alt="an id badge">
    <h1><%= title %></h1>
    <a href="/">Home</a>

    <p><%= "SERIAL NUMBER: #{profile.serial_number}" %></p>
    <p><%= "VALIDITY: #{profile.issue_at.strftime("%Y-%m-%d")} to #{profile.expire_at.strftime("%Y-%m-%d")}" %></p>
    <p><%= "ISSUER: #{profile.issuer.org} (#{profile.issuer.org_unit})" %></p>
    <p><%= "CRL: #{profile.crl_url}" %></p>
    <p><%= "SUBJECT ORG: #{profile.subject.org}" %></p>
    <p><%= "SUBJECT ORG UNIT: #{profile.subject.org_unit}" %></p>
    <p><%= "SUBJECT NAME: #{profile.subject.common_name}" %></p>
    <p><%= "SUBJECT UID: #{profile.subject.uid}" %></p>
    <p><%= "SUBJECT EMAIL: #{profile.subject_email_address}" %></p>

  </body>
</html>
