<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/">Home</a>

    <% if user[:certs].any? %>
      <h2>Certificates</h2>

      <% user[:certs].each do |cert| %>
        <code><%= cert %></code>
      <% end %>
    <% else %>
      <p>Couldn't find any certificates. Please <a href="/login">try again</a>.</p>
    <% end %>
  </body>
</html>
